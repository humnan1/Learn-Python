#Python cna be run on Pycharm or command promt. Command prompt good for leaning, lightweight, Pycharm is full featured with debuggers.
#IDE: Integrated developemtn envrionment special place to execute python code, e.g pycharm
#In pyhcar, new project an dthne new file

#This hash means this is a comment and wont be executed

print("Hello")
#Very basic python code that prints the string "hello"
#Console is where python outputs


#Variables store information, there are no spaces, so use underscore to separate words
character_name= "John"
print("My name is" + character_name)
#This prints "My name is John", The two strings are concatenated

#String variable is plain text in quoatation mark
#Integer is plain whole number
#Boolean variable is true or false value
#Float is decimal point

print("Learn\nPython")
#\n creates a new line 

user_name = input("What is your name? ")


phrase = "Leanr Python"
#Stored some string in my variable "phrase"
print(phrase.lower())
#This converts my string to lower case
print(phrase.upper())

#Checking if a string is upper or lower case
print(phrase.isupper())
print(phrase.islower())
#output should be a bool

print(phrase.upper().isupper())
#converts phrase to uppercase, then checks if it is upper case

print(len(phrase))
#find the length of pnrase
print(phrase[0])
#gives the first value/digit of the phrase, starts with 0, 1 is the second etc
# however you can go backwards, so -1 is the last value etc

print(phrase.index("L"))
#gives me the value of the capital letter L in our phrase variable

print(phrase.replace("nr", "rn"))
#replaces the letters nr with rn

print(10 % 3)
#modulus operator gives the remainder of 10/3

my_number = 5
print(str(my_number))
#converts my number (5) into a string, this "5" can nw be cncatenated with strings
#Normal integers cannot be cincatentaed with strings, so it has to be converted first

my_number = -8
#changed the value
print(abs(my_number))
#gives the absolute value of my numbr which is 8

print(pow(3, 2))
# this is 3^2 so its 9
print(max(3, 2))
#Tells us which number is bigger, min function does the opposit

print(round(6.7))
#rounds my numebr up to 7

from math import *
#grabs maths functios tat we can use. now we can access more

print(floor(3.6))
#chops off the decimal point
print(ceil(4.2))
#rounds the number up unconditionally, so this one wpuld be 4

print(sqrt(9))
#gives square root, so answer shuld be 3.0

#to get an input fro  user use input function
name = input("Enter name: ")
print("Hello " + name + "!")

new_number = "3"
print(int(new_number))
#this converted out string "3" into the number 3
print(float(new_number))
#This converted the number 3 into the float 3.0

colour = input("Enter a colour: ")
print("Roses are " + colour)

friends = ["Pip", "Chip", "Mip", "Kip"]
print(friends[1])
#That would print Chip
print(friends[1:])
#That would print Chip and Mip, as would print(friends[1:3])
# You can modify the list by:
friends[1] = "Kip"
#Now kip is the 1 value
new_friends = ["Tip", "Wip"]
friends.extend(new_friends)
print(friends)
#That extended the freinds list from the en
friends.insert(1, "Bip")
#Adds Bip to position 1
friends.remove("Kip")
#kip is not our freind anymore
friends.pop()
#This pops na item off the list
friends.clear()
#Thta would clear the list
friends.count("Chip")
#How many Chips are in the list, only one
friends.sort()
#Put in alphabetical order
friends.reverse()
#reverse the list order
friends2 = friends.copy()
#friends2 has the same atrubutes as friends

#Tuples can store multiple peices of information
coordinates = (4, 5)
print(coordinates[0])
#prints 4
#Tuple values cant be changed

def say_hi():
  print("Hi user")

say_hi()

#def is dfor functions and the next line has to be indented for it to e part of the function
#The last line calls/executre the functions
#if i add a name to the function inside the brackets it means every time i run the function i have to give a name:

def say_hi(name):
  print("Hi " + name)

say_hi("Chip")
say_hi("Mip")

#Can add more than one information

def say_hi(name, age):
  print("hi " + name + " you are " + age)

say_hi("Mip", "35")


def cube(number):
  return number*number*number

print(cube(3))

#Return gives us a value back


#Must be capital T True for Bool
#Notice the indentatio (Tab)
is_female = True
if is_female:
  print("You are a female")
else:
  print("You are not female")

# or operator works if at least one condition is True
# and operator only owrks if both/all conditions are true, so in the below scenario if or was replaced by and , you would have to be both male an dtall for the first message to print
# elif = else if
#not function negates what was in the brackets, so  it is checking if you are not tall

is_male = False
is_tall = True
if is_male or is_tall:
  print("You are male or tall or both")
elif is_male and not(is_tall):
  print("you are a short male")
else:
  print("your are not male or tall")

#Finding the biggest number
def max_num(num1, num2, num3)
  if num1 >= num2 and num1 >= num3:
    return num1
  elif num2 >= num1 and num2 >= num3:
    return num2
  else:
    return num3

print(max_num(4, 7, 2)
#This ouptuts the largest number

# == The same as
# != not equal to

num1 = float(input("Enter first number: "))
op = input("Enter operator: ")
num2 = float(input("Enter second number: "))
if op == "+":
  print(num1 + num2)
elif op == "-":
  print(num1 - num2)
elif op == "/":
  print(num1 / num2)
elif op == "*":
  print(num1 * num2)
else:
  print("invalid operator")


#Creating a dictionary for month conversions
#The dictionary will beplaced inside these curly brackets {}
#The first value (e.g. Jan) is called the key nd the second value (e.g. January) is called the value)

monthConversions = {
  "Jan": "January",
  "Feb": "February",
  "Mar": "March",
  "Apr": "April",
  "May": "May",
  "Jun": "June",
  "Jul": "July",
  "Aug": "August",
  "Sep": "September",
  "Oct": "October",
  "Nov": "November",
  "Dec": "December",
}

print(monthConversions["Nov"])
print(monthConversions.get("Dec"))
print(monthConversions.get("Luv", "Not valid Key"))
#keys canbe anything even numbers as long as they are all unique


#When creating a while loop creating a variable that is a numbr
#paying attention to things like indentation(Tab) and colons
i = 1
while i <= 10:
  print(i)
  i += 1   # Same as i = i + 1

print("finished loop")


#building a guessing game, kind of like a password

secret_word =  "Chip"
guess = ""
guess_count = 0
guess_limit = 3
out_of_guesses = False

while guess != secret_word and not(out_of_guesses):
  if guess_count < guess_limit:
    guess = input("Enter guess: ")
    guess_count += 1
  else:
    out_of_guesses = True

if out_of_guesses:
  print("Out of guesses, YOU LOSE!")

else:
  print("You win!")

print("Access granted")
# Like a password system, as long as user is not outguesses , orgets the password, while loop contuinues.


#For loops
friends = ["Chip", "Mip", "Pip", "Wip"]
for friends in friends:
  print(friends)

for numbers in range(10):
  print(numbers)

for numbers in range(3, 10)
  print(numbers)

for numbers in range(6)
  if index == 0:
    print("first iteration")
  else:
    print("Not first")

#Building exponent function

print(2**3) #2^3

def raise_to_power(base_num, pow_num):
  result = 1
  for index in range(pow_num):
    result = result * base_num
  return result

print(raise_to_power(3, 2))

number_grid = [ 
  [1, 2, 3]
  [4, 5, 6]
  [7, 8, 9]
  [0]
]

print(number_grid[0][0])  #print the row 0, column 0, which is 1


number_gridd = [ 
  [1, 2, 3]
  [4, 5, 6]
  [7, 8, 9]
  [0]
]

for row in number_gridd:
  for col in row: 
    print(col) #prints every value in every column

#build a basic transator for a secret language, every vowel is a h
 def translate(phrase):
  translation = ""
  for letter in phrase:
    if letter in "AEIOUaeiou"
      translation = translation + h
    else:
      translation = translation + letter
  return translation 

print(translate(input("Enter a phrase ")))

#try/except

try:
  number = int(input("enter a number: "))
  print(number)
except ZeroDivisionError as err:
  print(err)
except ValueError:
  print("Invalid input")

#Reading external files
employee_file = open( , ) #imnsert name of file to open the file as a stirng
# enter this before the frst comma, 
# after the first comman enter the mode you want tp open it as e.g. "r" is read only file, "w" can be change "a" append , infrmation can be added, "r+" read and write

print(employee_file.readable()) #check if the employee file is readable 
print(employee_file.readline()) #print the first line of ht efile, a
print(employee_file.readlines()) #take all the linesa dn put them in a list

for employee in employee_file.readlines():
print(employee)

employee_file.write("Toby -Human Resources") # add this to the end of th efile
employee_file.close() # close the file




class Student: # imahine this ina dfferent python file  file, it defines what a student is 

  def___init__(self, name, major, gpa, is_on_probation):
    self.name = name
    self.major = major
    self.gpa = gpa
    self.is_on_probation = is_on_probation

# back on python main
from Student import Student  #first student is efferein gto the file,a nd the secnd sudent is reffering to the class "student"

Student1 = Student("Chip", "Business", 3.1, False) #this is an object called student one
Student2 = Student("Mip", "Art", 2.5, True)
print(student1)
print(student2.gpa)


#Building a multiple choice quiz

class Question:    #In separate python file
  def_init_(self, prompt, answer):
    self.prompt = prompt
    self.answer = answer

#In main python file
from Question import Question

question_prompts = [
  "What colour are apples?\n(a) Red\n(b) Purple\n(c) Orange\n\n",
  "What colour are bananas\n(a) Magenta\n(b) BLue\n(c) Yellow\n\n",
  "What colour are strawberries\n(a) Yellow\n(b) Red\n(c) Blue\n\n"
]

questions = [
  Questions(question_prompts[0], "a")
  Questions(question_prompts[1], "c")
  Questions(question_prompts[2], "b")
]

def run_test(questions):
  score = 0
  for question in questions:
    answer = input(question.prompt)
    if answer == question.answer
      score += 1
  print("You got " + str(score)  + "/" + str(len(question))) + "Correct")

run_test(questions)












